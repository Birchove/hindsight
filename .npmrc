# Prefer a nearby registry when registry.npmjs.org is slow/unreachable.
# Override with: npm install --registry=https://registry.npmjs.org
registry=https://registry.npmmirror.com
fetch-timeout=600000
fetch-retries=5
maxsockets=5
